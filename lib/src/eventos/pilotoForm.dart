import 'package:flutter/material.dart';

class PilotoForm extends StatefulWidget {
  
  const PilotoForm({Key? key}) : super(key: key);

  @override
  _PilotoFormState createState() => _PilotoFormState();
}

class _PilotoFormState extends State<PilotoForm> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }

  Widget _pilototextForm() {
    return Column(
      children: const [
      ],
    );
  }

  Widget _textForm() {
    return TextFormField(
      
    );
  }
}

